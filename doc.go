// Package bindflag is a lightweight supplement to the built-in flag package.
// It provides a set of flag.Value types that are bound to existing memory
// locations but do not overwrite them with default values.
package bindflag
